#!/usr/bin/env node

var program = require('commander');
var path = require('path');
var pkg = require('../package.json');
var u3d = require('..');

// ==== version
program.version(pkg.version);

// ==== usage
program.usage('[options] <path>')

// ==== Option
program.option('-t, --trace', 'Show trace output');
program.option('-s, --start', 'Start the new Project');

program.on('--help', function(){
  console.log('  Examples:');
  console.log('');
  console.log('    $ u3d init');
  console.log('');
});


// ==== parse
program.parse(process.argv);

// ==== destination
var relativePath = program.args.shift();
var destinationPath = "";
if(relativePath) {
  destinationPath = path.join(process.cwd(), relativePath);
} else {
  destinationPath = process.cwd();
}

// ==== execute
u3d.u3d.init(program, destinationPath);
